#!/bin/sh
# Switches the gpu used for graphics on startup

notify-send '🎮 switch startup gpu';

if [ "$(gfxmode)" = 'nvidia' ]
then
	sudo envycontrol -s 'hybrid' --rtd3;
else
	sudo envycontrol -s 'nvidia' --force-comp;
fi

prompt 'Reboot computer?' 'sudo -A reboot';
